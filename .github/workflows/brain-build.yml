name: Second Brain build

on:
  push:
    branches: [main]
    paths:
      - "brain/**"
      - "public/scripts/analytics-tracker.js"
      - "scripts/build-brain.mjs"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/brain-build.yml"
  pull_request:
    branches: [main]
    paths:
      - "brain/**"
      - "public/scripts/analytics-tracker.js"
      - "scripts/build-brain.mjs"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/brain-build.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build Astro site and Quartz vault
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@a7487c7e89a18df4991f7f222e4898a00d66ddda # v4.1.0
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e # v4.3.0
        with:
          node-version: 22
          cache: pnpm

      - name: Install site dependencies
        run: pnpm install --frozen-lockfile

      - name: Build production site and Second Brain
        run: pnpm build
